# Configuration

## Критичні секрети

1. `TELEGRAM_BOT_TOKEN`
2. `ADMIN_API_KEY`
3. `DATABASE_URL`, `REDIS_URL`
4. `METACULUS_API_TOKEN` (для API доступу Metaculus)

## Duplicate

1. `SIGNAL_DUPLICATE_THRESHOLD`
2. `SIGNAL_DUPLICATE_BROAD_THRESHOLD`
3. `SIGNAL_DUPLICATE_BROAD_MIN_OVERLAP`
4. `SIGNAL_DUPLICATE_BROAD_MIN_JACCARD`
5. `SIGNAL_DUPLICATE_BROAD_MIN_WEIGHTED_OVERLAP`
6. `SIGNAL_DUPLICATE_BROAD_RELAXED_FUZZY_MIN`

## Arbitrage

1. `SIGNAL_MODE_MOMENTUM_MIN_MOVE`
2. `SIGNAL_MODE_UNCERTAINTY_MAX_SCORE`
3. `SIGNAL_ARBITRAGE_MIN_LIQUIDITY`
4. `SIGNAL_ARBITRAGE_MIN_VOLUME_24H`
5. `SIGNAL_ARBITRAGE_MIDPOINT_BAND`
6. `SIGNAL_ARBITRAGE_MAX_CANDIDATES`
7. `SIGNAL_ARBITRAGE_EXCLUDE_KEYWORDS`

## Rules fallback

1. `SIGNAL_RULES_RISK_THRESHOLD`
2. `SIGNAL_RULES_MISSING_MIN_LIQUIDITY`
3. `SIGNAL_RULES_MISSING_MIN_VOLUME_24H`
4. `SIGNAL_RULES_MISSING_DAILY_CAP`

## Top-selection / rollback

1. `SIGNAL_TOP_USE_V2_SELECTION`
2. `SIGNAL_TOP_V2_RANK_BY_SCORE_TOTAL`
3. `SIGNAL_TOP_MIN_SCORE_TOTAL`
4. `SIGNAL_TOP_MIN_UTILITY_SCORE`
5. `SIGNAL_TOP_MAX_MISSING_RULES_SHARE`
6. `SIGNAL_TOP_MIN_CONFIDENCE_MISSING_RULES`
7. `SIGNAL_TOP_ALLOW_FALLBACK_WHEN_EMPTY`
8. `SIGNAL_TOP_RULES_RISK_MIN_CONFIDENCE`
9. `SIGNAL_TOP_RULES_RISK_MIN_LIQUIDITY`
10. `SIGNAL_TOP_APPENDIX_C_ENABLED`
11. `SIGNAL_RANK_WEIGHT_EDGE`
12. `SIGNAL_RANK_WEIGHT_LIQUIDITY`
13. `SIGNAL_RANK_WEIGHT_EXECUTION_SAFETY`
14. `SIGNAL_RANK_WEIGHT_FRESHNESS`
15. `SIGNAL_RANK_WEIGHT_CONFIDENCE`

## Quality telemetry

1. `SNAPSHOT_FRESH_HOURS`
2. `TOP_WINDOW_SIZE`

## Stage 6 execution / agent

1. `SIGNAL_EXECUTION_MODEL` (`v1`/`v2`)
2. `SIGNAL_EXECUTION_V2_HORIZON`
3. `SIGNAL_EXECUTION_V2_LOOKBACK_DAYS`
4. `SIGNAL_EXECUTION_V2_MIN_SAMPLES`
5. `SIGNAL_EXECUTION_POSITION_SIZE_USD`
6. `SIGNAL_EXECUTION_POLYMARKET_MODE` (`gamma_api`/`clob_api`)
7. `SIGNAL_EXECUTION_POLYMARKET_GAS_FEE_USD`
8. `SIGNAL_EXECUTION_POLYMARKET_BRIDGE_FEE_USD`
9. `AGENT_POLICY_KEEP_EV_THRESHOLD_PCT` (default `0.003` — 0.3%)
10. `AGENT_POLICY_MODIFY_EV_THRESHOLD_PCT` (default `0.001`)
11. `AGENT_POLICY_MIN_CONFIDENCE` (default `0.35`)
12. `AGENT_POLICY_MIN_LIQUIDITY`
13. `AGENT_POLICY_VERSION`

## Stage 7 LLM провайдери

1. `STAGE7_AGENT_REAL_CALLS_ENABLED` — `true` = реальні LLM-виклики через FallbackAdapter (groq→gemini→openrouter); `false` = PlainApiAdapter (детерміністичний, без мережі)
2. `STAGE7_AGENT_PROVIDER` (`openai_compatible` / `langgraph`)
3. `STAGE7_AGENT_PROVIDER_PROFILE` (`gemini` / `groq` / `openrouter`) — профіль одного провайдера; при `real_calls=true` ігнорується, бо використовується FallbackAdapter
4. `STAGE7_GEMINI_MODEL` (default `gemini-2.5-flash`)
5. `STAGE7_GROQ_MODEL` (default `llama-3.3-70b-versatile`)
6. `STAGE7_OPENROUTER_MODEL` (default `google/gemini-2.5-flash`)
7. `STAGE7_AGENT_INTERNAL_GATE_PROFILE` (`permissive` / `balanced` / `strict`)
8. `STAGE7_AGENT_MONTHLY_BUDGET_USD`
9. `STAGE7_AGENT_COST_PER_CALL_USD`

## Polymarket CLOB

1. `POLYMARKET_CLOB_ENABLED` — увімкнути отримання bid/ask через CLOB API
2. `POLYMARKET_CLOB_API_BASE_URL`
3. `POLYMARKET_CLOB_API_KEY`
4. Порогова ліквідність для CLOB-виклику задана хардкодом у колекторі: `>= $1000` (max 3000 ринків за цикл)

## Logging

1. `LOG_DIR` (default `/app/logs`) — директорія для ротованих лог-файлів
2. Ротація: 50 MB на файл, зберігати 5 файлів (максимум 250 MB)
